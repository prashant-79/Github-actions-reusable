name: Main Pipeline
on:
  push:
    branches:
      - main
  pull_request:

jobs:
  call-reusable:
    uses: ./.github/workflows/reusable-build.yml
    with:
      node-version: "20"
      run-tests: true
